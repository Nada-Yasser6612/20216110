pipeline {
    agent any
    stages {
        stage('List Files') {
            steps {
                // For Windows, use 'bat' to execute commands
                bat 'dir'
            }
        }
    }
}
